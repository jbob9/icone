---
import { ArrowUpRight } from './icons';
---

    <section class="py-20 px-4 md:px-12 max-w-7xl mx-auto my-12 bg-brand-cream rounded-[3rem] text-brand-dark">
      <div class="text-center mb-12">
        <span class="text-brand-dark/60 font-bold uppercase text-xs tracking-widest mb-2 block">Testimonials</span>
        <h2 class="text-5xl md:text-7xl font-display uppercase leading-none">
          What Our <span class="text-brand-green">Clients Say</span>
        </h2>
        <p class="mt-4 text-gray-500 text-sm md:text-base max-w-lg mx-auto">
          Real feedback from the clients and brands we've partnered with to achieve extraordinary results.
        </p>
      </div>

      <div class="relative bg-white rounded-3xl p-6 md:p-12 shadow-xl border border-gray-100 flex flex-col md:flex-row gap-8 items-center">
        
        {/* Decorative Quote Mark */}
        <div class="absolute -top-6 -right-6 text-brand-yellow text-9xl font-display opacity-50 select-none">”</div>
        
        <div class="w-full md:w-1/2">
           <div class="flex items-center gap-3 mb-6">
              <span class="w-8 h-8 flex items-center justify-center bg-gray-100 rounded-full text-lg">🌸</span>
              <span class="font-display uppercase text-xl tracking-wider">Bloomistry</span>
           </div>
           
           <blockquote class="text-xl md:text-2xl font-medium leading-relaxed mb-8">
             “Brightcove transformed our digital presence with a stunning campaign that resonated perfectly with our audience. The attention to detail and creative direction was unmatched.”
           </blockquote>

           <button class="flex items-center gap-2 text-sm font-bold uppercase border-b border-brand-dark pb-1 hover:text-gray-600 transition-colors">
              Read the story <ArrowUpRight className="w-4 h-4" />
           </button>
        </div>

        <div class="w-full md:w-1/2 relative h-[300px] md:h-[400px] rounded-2xl overflow-hidden group cursor-pointer">
           <img src="https://picsum.photos/seed/testimonial/600/800" alt="Client Testimonial Video" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
           <div class="absolute inset-0 bg-black/20 group-hover:bg-black/10 transition-colors"></div>
           
           {/* Play Button */}
           <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-16 h-16 bg-brand-yellow/90 backdrop-blur rounded-full flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-brand-dark ml-1" fill="currentColor" viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg>
           </div>
           
           <div class="absolute bottom-6 left-6 text-white">
              <div class="font-bold text-lg">Mark Reynolds</div>
              <div class="text-xs uppercase opacity-80">Marketing Director</div>
           </div>
        </div>

      </div>

      {/* Pagination dots simulation */}
      <div class="flex justify-center gap-2 mt-8">
         <div class="w-8 h-2 bg-brand-dark rounded-full"></div>
         <div class="w-2 h-2 bg-gray-300 rounded-full"></div>
         <div class="w-2 h-2 bg-gray-300 rounded-full"></div>
      </div>
    </section>