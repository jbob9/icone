---
import { StarScribble } from './icons';
---

    <section id="home" class="relative pt-32 pb-20 px-4 md:px-12 max-w-7xl mx-auto overflow-hidden min-h-screen flex flex-col items-center">
      
      {/* Brand Logo - Top Center Absolute */}
      <div class="absolute top-6 left-1/2 transform -translate-x-1/2 md:left-12 md:translate-x-0 flex items-center gap-2">
        <div class="text-2xl font-bold font-display tracking-wider text-brand-cream uppercase">Brightcove.</div>
      </div>

      <div class="w-full text-center mt-8 md:mt-16 z-10 relative">
        <h1 class="text-6xl md:text-8xl lg:text-9xl font-display uppercase leading-[0.9] text-brand-cream relative inline-block">
          Bringing <span class="relative inline-block z-10">
            Your
             {/* Yellow lightning bolt/scribble decoration */}
            <span class="absolute -top-4 -right-8 text-brand-yellow w-12 h-12 hidden md:block">
               <StarScribble className="w-12 h-12 rotate-12" />
            </span>
          </span><br />
          Vision to Life.
        </h1>
        
        <p class="mt-8 max-w-lg mx-auto text-gray-300 text-sm md:text-base font-medium leading-relaxed">
          Combining creativity and strategy to help brands stand out and achieve their goals. We craft unique solutions that help brands connect.
        </p>

        <button class="mt-8 px-8 py-3 bg-brand-yellow text-brand-dark font-bold rounded-full uppercase text-sm hover:scale-105 transition-transform duration-300 shadow-[0_0_20px_rgba(219,232,44,0.3)]">
          Let's Collab!
        </button>
      </div>
    </section>