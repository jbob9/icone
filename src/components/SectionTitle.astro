---
import { cn } from '../lib/utils';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<h2 class={cn("text-3xl md:text-4xl font-bold mb-12" ,className)}>
    <slot/>
    <span class="inline-block ml-1">_</span>
  </h2>