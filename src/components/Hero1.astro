---
import { StarScribble } from './icons';
---

    <section id="home" class="relative pt-32 pb-20 px-4 md:px-12 max-w-7xl mx-auto overflow-hidden min-h-screen flex flex-col items-center">
      
      {/* Brand Logo - Top Center Absolute */}
      <div class="absolute top-6 left-1/2 transform -translate-x-1/2 md:left-12 md:translate-x-0 flex items-center gap-2">
        <div class="text-2xl font-bold font-display tracking-wider text-brand-cream uppercase">Brightcove.</div>
      </div>

      <div class="w-full text-center mt-8 md:mt-16 z-10 relative">
        <h1 class="text-6xl md:text-8xl lg:text-9xl font-display uppercase leading-[0.9] text-brand-cream relative inline-block">
          Bringing <span class="relative inline-block z-10">
            Your
             {/* Yellow lightning bolt/scribble decoration */}
            <span class="absolute -top-4 -right-8 text-brand-yellow w-12 h-12 hidden md:block">
               <StarScribble className="w-12 h-12 rotate-12" />
            </span>
          </span><br />
          Vision to Life.
        </h1>
        
        <p class="mt-8 max-w-lg mx-auto text-gray-300 text-sm md:text-base font-medium leading-relaxed">
          Combining creativity and strategy to help brands stand out and achieve their goals. We craft unique solutions that help brands connect.
        </p>

        <button class="mt-8 px-8 py-3 bg-brand-yellow text-brand-dark font-bold rounded-full uppercase text-sm hover:scale-105 transition-transform duration-300 shadow-[0_0_20px_rgba(219,232,44,0.3)]">
          Let's Collab!
        </button>
      </div>

      {/* Hero Visuals */}
      <div class="mt-16 relative w-full max-w-4xl h-[400px] md:h-[600px]">
        {/* Background Graphic Element */}
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[120%] bg-brand-green/30 blur-3xl rounded-full pointer-events-none"></div>

        {/* Floating Images */}
        <div class="absolute top-0 left-4 md:left-20 w-48 md:w-72 h-64 md:h-80 bg-gray-800 rounded-3xl overflow-hidden shadow-2xl transform -rotate-6 hover:rotate-0 transition-all duration-500 z-10 border-4 border-brand-dark/20">
            <img src="https://picsum.photos/seed/design1/600/800" alt="Work sample" class="w-full h-full object-cover" />
             <div class="absolute bottom-4 left-4 right-4 bg-brand-cream/90 backdrop-blur p-3 rounded-xl">
                 <div class="text-brand-dark font-bold text-xs uppercase">Visual Identity</div>
             </div>
        </div>

        <div class="absolute top-12 right-4 md:right-20 w-56 md:w-80 h-72 md:h-96 bg-gray-800 rounded-3xl overflow-hidden shadow-2xl transform rotate-6 hover:rotate-0 transition-all duration-500 z-20 border-4 border-brand-dark/20">
             <img src="https://picsum.photos/seed/product2/600/800" alt="Work sample 2" class="w-full h-full object-cover" />
             <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
                 <h2 class="text-4xl md:text-6xl font-display uppercase text-white drop-shadow-lg text-center leading-none">Refresh<br/>ing</h2>
             </div>
        </div>
        
        {/* Decor items */}
        <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 bg-brand-lime text-brand-dark px-4 py-2 rounded-lg font-bold uppercase text-xs rotate-[-12deg] shadow-lg z-30">
            Brand Strategy
        </div>
      </div>
    </section>